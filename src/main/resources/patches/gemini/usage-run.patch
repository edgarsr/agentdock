{
  "pathGlob": "bundle/gemini-*.js",
  "find": "  const remoteAdminSettings = partialConfig.getRemoteAdminSettings();",
  "replace": "  if (argv[\"usage-json\"] || process.env[\"GEMINI_CLI_USAGE_JSON\"] === \"true\") {\n    await outputUsageJson(partialConfig);\n  }\n  const remoteAdminSettings = partialConfig.getRemoteAdminSettings();",
  "alreadyApplied": "GEMINI_CLI_USAGE_JSON"
}
